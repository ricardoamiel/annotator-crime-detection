# Copy this to .env.local for local development
# Vercel will use VITE_API_URL set in its dashboard

# Local dev: leave empty (vite proxy handles it)
VITE_API_URL=

# Production (set this in Vercel dashboard, NOT here):
# VITE_API_URL=https://anotador-backend.onrender.com/api